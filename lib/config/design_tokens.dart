import 'package:flutter/material.dart';

/// Couleurs du thème sombre (par défaut)
class ChillColorsDark {
  static const bgPrimary = Color(0xFF08090A);
  static const bgElevated = Color(0xFF111214);
  static const bgSurface = Color(0xFF161719);
  static const border = Color(0xFF1E2025);
  static const borderSubtle = Color(0xFF151618);
  static const textPrimary = Color(0xFFF7F8F8);
  static const textSecondary = Color(0xFF8A8F98);
  static const textMuted = Color(0xFF5C6170);
  static const accent = Color(0xFF0D9668);
  static const accentHover = Color(0xFF2ABF8A);
  static const accentGlow = Color(0x260D9668); // 15% opacity
  static const blue = Color(0xFF4EA7FC);
  static const red = Color(0xFFEB5757);
  static const green = Color(0xFF3DA070);
  static const orange = Color(0xFFFC7840);
}

/// Couleurs du thème clair
class ChillColorsLight {
  static const bgPrimary = Color(0xFFF0F1F3);
  static const bgElevated = Color(0xFFFAFAFA);
  static const bgSurface = Color(0xFFEDEEF0);
  static const border = Color(0xFFDEE0E5);
  static const borderSubtle = Color(0xFFE8E9EC);
  static const textPrimary = Color(0xFF111214);
  static const textSecondary = Color(0xFF6B7280);
  static const textMuted = Color(0xFF9CA3AF);
  static const accent = Color(0xFF047D56);
  static const accentHover = Color(0xFF036647);
  static const accentGlow = Color(0x1A047D56); // 10% opacity
  static const blue = Color(0xFF2563EB);
  static const red = Color(0xFFDC2626);
  static const green = Color(0xFF128A3E);
  static const orange = Color(0xFFEA580C);
}

/// Rayons de bordure
class ChillRadius {
  static const double sm = 4;
  static const double md = 6;
  static const double lg = 8;
  static const double xl = 12;
  static const double xxl = 16;
}

/// Espacements
class ChillSpacing {
  static const double pageMaxWidth = 1024;
}
